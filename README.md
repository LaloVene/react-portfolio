# React Portfolio

Personal portfolio built with React, Vite, and Deno.

## Scripts

- `npm run dev` starts the Deno-backed Vite dev server.
- `npm run build` creates a production build in `dist/`.
- `npm run preview` serves the production build locally.
